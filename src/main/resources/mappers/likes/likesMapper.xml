<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Keyword.mappers.likes.LikesMapper">
    <insert id="create">
        INSERT INTO likes (
            content_id
            , user_id
        ) VALUES (
            #{contentId}
            , #{userId}
        )
    </insert>

    <delete id="delete">
        DELETE FROM likes
        WHERE content_id = #{contentId} AND user_id = #{userId}
    </delete>

    <select id="isLiked" resultMap="LikesResultMap">
        SELECT content_id
            , user_id
            , reg_date
        FROM likes
        WHERE content_id = #{contentId} AND user_id = #{userId}
    </select>

    <select id="getMyLikes" resultMap="LikesResultMap">
        SELECT content_id
            , user_id
            , reg_date
        FROM likes
        WHERE user_id = #{userId}
    </select>

    <resultMap id="LikesResultMap" type="LikesVO">
        <id property="contentId" column="content_id"/>
        <result property="userId" column="user_id"/>
        <result property="regDate" column="reg_date"/>
    </resultMap>
</mapper>