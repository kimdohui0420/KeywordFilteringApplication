<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/aop https://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- annotation 인식 -->
    <mvc:annotation-driven/>

    <!--정적자원 매핑-->
    <mvc:resources mapping="/resources/**" location="/resources/"/>
    <mvc:resources mapping="/dist/**" location="/resources/dist/"/>
    <mvc:resources mapping="/scripts/**" location="/resources/scripts/"/>
    <mvc:resources mapping="/src/**" location="/resources/src/"/>
    <mvc:resources mapping="/slick/**" location="/resources/slick/"/>

    <!-- viewResolver 설정 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/"/>
        <property name="suffix" value=".jsp"/>
    </bean>

    <!-- Component 패키지 지정 -->
    <context:component-scan base-package="Keyword"/>

    <!-- AOP 설정 -->
    <aop:aspectj-autoproxy/>

    <!-- 로그인, 회원가입 -->
    <bean id="loginInterceptor" class="Keyword.commons.interceptor.LoginInterceptor"/>
    <bean id="authInterceptor" class="Keyword.commons.interceptor.AuthInterceptor"/>
    <bean id="rememberMeInterceptor" class="Keyword.commons.interceptor.RememberMeInterceptor"/>
    <bean id="loginAfterInterceptor" class="Keyword.commons.interceptor.LoginAfterInterceptor"/>
    <mvc:interceptors>
        <mvc:interceptor>
            <mvc:mapping path="/user/loginPost"/>
            <ref bean="loginInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/user/myInfo"/>
            <mvc:mapping path="/user/myLikes"/>
            <ref bean="authInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**/"/>
            <ref bean="rememberMeInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/user/login"/>
            <mvc:mapping path="/user/signUp"/>
            <ref bean="loginAfterInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 파일 업로드 -->
    <!--
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="maxUploadSize" value="10485760"/>
    </bean>
    -->
</beans>